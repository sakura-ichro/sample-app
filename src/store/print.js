'use strict'

// ------------------------------
export const state = () => ({
  preview: false
})

// ------------------------------
export const getters = {}

// ------------------------------
export const mutations = {
  PREVIRW(state, res) {
    state.preview = res
  }
}

// ------------------------------
export const actions = {}
